<div class="container my-5">
  <div class="row">
    <div class="col-6 mx-auto">
      <% @restaurants.each do |restaurant| %>
        <div>
          <p><%= link_to restaurant.name, restaurant_path(restaurant), class: 'btn btn-primary' %> <%= restaurant.address%></p>
        </div>
      <% end %>
    </div>
  </div>
</div>
